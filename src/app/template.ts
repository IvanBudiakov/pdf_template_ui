export interface Template {
    id : number,
    serviceCode : number,
    name : string,
    srcLink : string,
    expiryDate : Date,
    effectiveDate : Date,
    createdDate : Date,
    modifiedDate : Date
}
